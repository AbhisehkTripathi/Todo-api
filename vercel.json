{
    "version": 2,
    "builds": [
      {
        "src": "index.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/$1"
      }
    ],
    "env": {
      "MONGODB_URI": "mongodb://localhost:27017/crudDB",
      "OTHER_ENV_VARIABLE": "value"
    }
  }
  